{
  "name": "dynamodb-on-local",
  "description": "Using DynamoDB on local machine",
  "scripts": {
    "start": "DYNAMO_ENDPOINT=http://localhost:8000 AWS_REGION=ap-northeast-1 AWS_ACCESS_KEY_ID=local AWS_SECRET_ACCESS_KEY=local dynamodb-admin --open --port=8001"
  },
  "dependencies": {
    "dynamodb-admin": "^3.1.2"
  }
}
